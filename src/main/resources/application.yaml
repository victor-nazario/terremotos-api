server:
  port: 8085
  servlet:
    context-path:
    session:
      timeout: 10m


spring.jpa.properties.hibernate.dialect:  org.hibernate.dialect.MySQL5InnoDBDialect

spring.jackson.serialization.indent-output: true

spring:
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    hikari:
      connection-test-query: select 1 from dual
      driver-class-name: com.mysql.cj.jdbc.Driver


    type: com.zaxxer.hikari.HikariDataSource
    platform: mysql
    url: jdbc:mysql://b42f47451b8532:e143d8a8@us-cdbr-iron-east-01.cleardb.net/heroku_8e6bdf534d413b1?reconnect=true
    username: root
    password: root

logging:
  level:
    ROOT: INFO